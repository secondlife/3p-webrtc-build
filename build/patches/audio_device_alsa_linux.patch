diff -dpurN src/modules/audio_device/linux/audio_device_alsa_linux.cc src-patched/modules/audio_device/linux/audio_device_alsa_linux.cc
--- src/modules/audio_device/linux/audio_device_alsa_linux.cc	2026-07-03 00:17:11.180714333 +0200
+++ src-patched/modules/audio_device/linux/audio_device_alsa_linux.cc	2026-07-02 23:44:32.572864164 +0200
@@ -110,7 +110,9 @@ AudioDeviceLinuxALSA::AudioDeviceLinuxAL
       _recording(false),
       _playing(false),
       _recIsInitialized(false),
+      _recIsStopping(false),
       _playIsInitialized(false),
+      _playIsStopping(false),
       _recordingDelay(0),
       _playoutDelay(0) {
   memset(_oldKeyState, 0, sizeof(_oldKeyState));
@@ -448,7 +450,7 @@ int32_t AudioDeviceLinuxALSA::StereoReco
     InitRecordingLocked();
   }
   if (recording) {
-    StartRecording();
+    StartRecordingLocked();
   }
 
   return 0;
@@ -508,7 +510,7 @@ int32_t AudioDeviceLinuxALSA::StereoPlay
     InitPlayoutLocked();
   }
   if (playing) {
-    StartPlayout();
+    StartPlayoutLocked();
   }
 
   return 0;
@@ -759,7 +761,7 @@ int32_t AudioDeviceLinuxALSA::InitPlayou
 int32_t AudioDeviceLinuxALSA::InitPlayoutLocked() {
   int errVal = 0;
 
-  if (_playing) {
+  if (_playing || _playIsStopping) {
     return -1;
   }
 
@@ -880,7 +882,7 @@ int32_t AudioDeviceLinuxALSA::InitRecord
 int32_t AudioDeviceLinuxALSA::InitRecordingLocked() {
   int errVal = 0;
 
-  if (_recording) {
+  if (_recording || _recIsStopping) {
     return -1;
   }
 
@@ -1015,16 +1017,27 @@ int32_t AudioDeviceLinuxALSA::InitRecord
 }
 
 int32_t AudioDeviceLinuxALSA::StartRecording() {
+  MutexLock lock(&mutex_);
+  return StartRecordingLocked();
+}
+
+int32_t AudioDeviceLinuxALSA::StartRecordingLocked() {
+  if (_recIsStopping) {
+    return -1;
+  }
+
   if (!_recIsInitialized) {
     return -1;
   }
 
+  if (_handleRecord == nullptr) {
+    return -1;
+  }
+
   if (_recording) {
     return 0;
   }
 
-  _recording = true;
-
   int errVal = 0;
   _recordingFramesLeft = _recordingFramesIn10MS;
 
@@ -1033,17 +1046,8 @@ int32_t AudioDeviceLinuxALSA::StartRecor
     _recordingBuffer = new int8_t[_recordingBufferSizeIn10MS];
   if (!_recordingBuffer) {
     RTC_LOG(LS_ERROR) << "failed to alloc recording buffer";
-    _recording = false;
     return -1;
   }
-  // RECORDING
-  _ptrThreadRec = PlatformThread::SpawnJoinable(
-      [this] {
-        while (RecThreadProcess()) {
-        }
-      },
-      "webrtc_audio_module_capture_thread",
-      ThreadAttributes().SetPriority(ThreadPriority::kRealtime));
 
   errVal = LATE(snd_pcm_prepare)(_handleRecord);
   if (errVal < 0) {
@@ -1061,11 +1065,22 @@ int32_t AudioDeviceLinuxALSA::StartRecor
     if (errVal < 0) {
       RTC_LOG(LS_ERROR) << "capture snd_pcm_start 2nd try err: "
                         << LATE(snd_strerror)(errVal);
-      StopRecording();
+      StopRecordingLocked();
       return -1;
     }
   }
 
+  _recording = true;
+
+  // RECORDING
+  _ptrThreadRec = PlatformThread::SpawnJoinable(
+      [this] {
+        while (RecThreadProcess()) {
+        }
+      },
+      "webrtc_audio_module_capture_thread",
+      ThreadAttributes().SetPriority(ThreadPriority::kRealtime));
+
   return 0;
 }
 
@@ -1075,19 +1090,31 @@ int32_t AudioDeviceLinuxALSA::StopRecord
 }
 
 int32_t AudioDeviceLinuxALSA::StopRecordingLocked() {
+  if (_recIsStopping) {
+    return 0;
+  }
+
   if (!_recIsInitialized) {
+    _recording = false;
     return 0;
   }
 
   if (_handleRecord == nullptr) {
+    _recIsInitialized = false;
+    _recording = false;
     return -1;
   }
 
   // Make sure we don't start recording (it's asynchronous).
   _recIsInitialized = false;
   _recording = false;
+  _recIsStopping = true;
 
+  // The capture thread can reacquire mutex_ after DeliverRecordedData().
+  // Join it without holding the mutex to avoid a stop/delivery deadlock.
+  mutex_.Unlock();
   _ptrThreadRec.Finalize();
+  mutex_.Lock();
 
   _recordingFramesLeft = 0;
   if (_recordingBuffer) {
@@ -1095,19 +1122,24 @@ int32_t AudioDeviceLinuxALSA::StopRecord
     _recordingBuffer = nullptr;
   }
 
+  int32_t result = 0;
+
   // Stop and close pcm recording device.
   int errVal = LATE(snd_pcm_drop)(_handleRecord);
   if (errVal < 0) {
     RTC_LOG(LS_ERROR) << "Error stop recording: " << LATE(snd_strerror)(errVal);
-    return -1;
+    result = -1;
   }
 
   errVal = LATE(snd_pcm_close)(_handleRecord);
   if (errVal < 0) {
     RTC_LOG(LS_ERROR) << "Error closing record sound device, error: "
                       << LATE(snd_strerror)(errVal);
-    return -1;
+    result = -1;
   }
+  // Null the handle regardless: on close failure the handle is in an
+  // undefined state and not safe to reuse.
+  _handleRecord = nullptr;
 
   // Check if we have muted and unmute if so.
   bool muteEnabled = false;
@@ -1116,9 +1148,8 @@ int32_t AudioDeviceLinuxALSA::StopRecord
     SetMicrophoneMute(false);
   }
 
-  // set the pcm input handle to NULL
-  _handleRecord = nullptr;
-  return 0;
+  _recIsStopping = false;
+  return result;
 }
 
 bool AudioDeviceLinuxALSA::RecordingIsInitialized() const {
@@ -1134,25 +1165,45 @@ bool AudioDeviceLinuxALSA::PlayoutIsInit
 }
 
 int32_t AudioDeviceLinuxALSA::StartPlayout() {
+  MutexLock lock(&mutex_);
+  return StartPlayoutLocked();
+}
+
+int32_t AudioDeviceLinuxALSA::StartPlayoutLocked() {
+  if (_playIsStopping) {
+    return -1;
+  }
+
   if (!_playIsInitialized) {
     return -1;
   }
 
+  if (_handlePlayout == nullptr) {
+    return -1;
+  }
+
   if (_playing) {
     return 0;
   }
 
-  _playing = true;
-
   _playoutFramesLeft = 0;
   if (!_playoutBuffer)
     _playoutBuffer = new int8_t[_playoutBufferSizeIn10MS];
   if (!_playoutBuffer) {
     RTC_LOG(LS_ERROR) << "failed to alloc playout buf";
-    _playing = false;
     return -1;
   }
 
+  int errVal = LATE(snd_pcm_prepare)(_handlePlayout);
+  if (errVal < 0) {
+    RTC_LOG(LS_ERROR) << "playout snd_pcm_prepare failed ("
+                      << LATE(snd_strerror)(errVal) << ")\n";
+    // just log error
+    // if snd_pcm_open fails will return -1
+  }
+
+  _playing = true;
+
   // PLAYOUT
   _ptrThreadPlay = PlatformThread::SpawnJoinable(
       [this] {
@@ -1162,14 +1213,6 @@ int32_t AudioDeviceLinuxALSA::StartPlayo
       "webrtc_audio_module_play_thread",
       ThreadAttributes().SetPriority(ThreadPriority::kRealtime));
 
-  int errVal = LATE(snd_pcm_prepare)(_handlePlayout);
-  if (errVal < 0) {
-    RTC_LOG(LS_ERROR) << "playout snd_pcm_prepare failed ("
-                      << LATE(snd_strerror)(errVal) << ")\n";
-    // just log error
-    // if snd_pcm_open fails will return -1
-  }
-
   return 0;
 }
 
@@ -1179,40 +1222,57 @@ int32_t AudioDeviceLinuxALSA::StopPlayou
 }
 
 int32_t AudioDeviceLinuxALSA::StopPlayoutLocked() {
+  if (_playIsStopping) {
+    return 0;
+  }
+
   if (!_playIsInitialized) {
+    _playing = false;
     return 0;
   }
 
   if (_handlePlayout == nullptr) {
+    _playIsInitialized = false;
+    _playing = false;
     return -1;
   }
 
+  _playIsInitialized = false;
   _playing = false;
+  _playIsStopping = true;
 
-  // stop playout thread first
+  // The playout thread can reacquire mutex_ after RequestPlayoutData().
+  // Join it without holding the mutex to avoid a stop/playout deadlock.
+  mutex_.Unlock();
   _ptrThreadPlay.Finalize();
+  mutex_.Lock();
 
   _playoutFramesLeft = 0;
   delete[] _playoutBuffer;
   _playoutBuffer = nullptr;
 
+  int32_t result = 0;
+
   // stop and close pcm playout device
   int errVal = LATE(snd_pcm_drop)(_handlePlayout);
   if (errVal < 0) {
     RTC_LOG(LS_ERROR) << "Error stop playing: " << LATE(snd_strerror)(errVal);
+    result = -1;
   }
 
   errVal = LATE(snd_pcm_close)(_handlePlayout);
-  if (errVal < 0)
+  if (errVal < 0) {
     RTC_LOG(LS_ERROR) << "Error closing playout sound device, error: "
                       << LATE(snd_strerror)(errVal);
-
-  // set the pcm input handle to NULL
-  _playIsInitialized = false;
+    result = -1;
+  }
+  // Null the handle regardless: on close failure the handle is in an
+  // undefined state and not safe to reuse.
   _handlePlayout = nullptr;
   RTC_LOG(LS_VERBOSE) << "handle_playout is now set to NULL";
 
-  return 0;
+  _playIsStopping = false;
+  return result;
 }
 
 int32_t AudioDeviceLinuxALSA::PlayoutDelay(uint16_t& delayMS) const {
@@ -1466,14 +1526,16 @@ int32_t AudioDeviceLinuxALSA::ErrorRecov
 // ============================================================================
 
 bool AudioDeviceLinuxALSA::PlayThreadProcess() {
-  if (!_playing)
-    return false;
-
   int err;
   snd_pcm_sframes_t frames;
   snd_pcm_sframes_t avail_frames;
 
   Lock();
+  if (!_playing || _handlePlayout == nullptr) {
+    UnLock();
+    return false;
+  }
+
   // return a positive number of frames ready otherwise a negative error code
   avail_frames = LATE(snd_pcm_avail_update)(_handlePlayout);
   if (avail_frames < 0) {
@@ -1498,6 +1560,10 @@ bool AudioDeviceLinuxALSA::PlayThreadPro
     UnLock();
     _ptrAudioBuffer->RequestPlayoutData(_playoutFramesIn10MS);
     Lock();
+    if (!_playing || _handlePlayout == nullptr) {
+      UnLock();
+      return false;
+    }
 
     _playoutFramesLeft = _ptrAudioBuffer->GetPlayoutData(_playoutBuffer);
     RTC_DCHECK_EQ(_playoutFramesLeft, _playoutFramesIn10MS);
@@ -1528,15 +1594,17 @@ bool AudioDeviceLinuxALSA::PlayThreadPro
 }
 
 bool AudioDeviceLinuxALSA::RecThreadProcess() {
-  if (!_recording)
-    return false;
-
   int err;
   snd_pcm_sframes_t frames;
   snd_pcm_sframes_t avail_frames;
-  std::vector<int8_t> buffer(_recordingBufferSizeIn10MS);
 
   Lock();
+  if (!_recording || _handleRecord == nullptr) {
+    UnLock();
+    return false;
+  }
+
+  std::vector<int8_t> buffer(_recordingBufferSizeIn10MS);
 
   // return a positive number of frames ready otherwise a negative error code
   avail_frames = LATE(snd_pcm_avail_update)(_handleRecord);
@@ -1621,6 +1689,10 @@ bool AudioDeviceLinuxALSA::RecThreadProc
       UnLock();
       _ptrAudioBuffer->DeliverRecordedData();
       Lock();
+      if (!_recording) {
+        UnLock();
+        return false;
+      }
     }
   }
 
diff -dpurN src/modules/audio_device/linux/audio_device_alsa_linux.h src-patched/modules/audio_device/linux/audio_device_alsa_linux.h
--- src/modules/audio_device/linux/audio_device_alsa_linux.h	2026-07-03 00:17:11.180714333 +0200
+++ src-patched/modules/audio_device/linux/audio_device_alsa_linux.h	2026-07-02 23:44:50.082863956 +0200
@@ -133,8 +133,10 @@ class AudioDeviceLinuxALSA : public Audi
       RTC_LOCKS_EXCLUDED(mutex_) override;
 
  private:
+  int32_t StartRecordingLocked() RTC_EXCLUSIVE_LOCKS_REQUIRED(mutex_);
   int32_t InitRecordingLocked() RTC_EXCLUSIVE_LOCKS_REQUIRED(mutex_);
   int32_t StopRecordingLocked() RTC_EXCLUSIVE_LOCKS_REQUIRED(mutex_);
+  int32_t StartPlayoutLocked() RTC_EXCLUSIVE_LOCKS_REQUIRED(mutex_);
   int32_t StopPlayoutLocked() RTC_EXCLUSIVE_LOCKS_REQUIRED(mutex_);
   int32_t InitPlayoutLocked() RTC_EXCLUSIVE_LOCKS_REQUIRED(mutex_);
   int32_t InitSpeakerLocked() RTC_EXCLUSIVE_LOCKS_REQUIRED(mutex_);
@@ -200,7 +202,9 @@ class AudioDeviceLinuxALSA : public Audi
   bool _recording;
   bool _playing;
   bool _recIsInitialized;
+  bool _recIsStopping;
   bool _playIsInitialized;
+  bool _playIsStopping;
 
   snd_pcm_sframes_t _recordingDelay;
   snd_pcm_sframes_t _playoutDelay;
