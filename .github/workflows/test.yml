name: test

on:
  release:
    types: [published]

jobs:
  package-macos:
    strategy:
      matrix:
        os: [macos-11]
        addrsize: ['64']
    runs-on: ${{ matrix.os }}
    steps:
    - name: Determine Release Tag
      env:
        RELEASE: ${{ github.ref_name }}
      id: release_tag
      run: |
        echo "tag=${RELEASE%%.*}" >> "$GITHUB_OUTPUT"
        echo "RELEASE=${RELEASE}" >> "$GITHUB_OUTPUT"
    - uses: secondlife/action-autobuild@v3.8.0
      with:
        addrsize: ${{ matrix.addrsize }}
        token: ${{ github.token }}
        build-args: ../build-cmd.sh 1332412191
  package-windows:
    strategy:
      matrix:
        os: [windows-2022]
        addrsize: ['64']
    runs-on: ${{ matrix.os }}
    steps:
    - name: Determine Release Tag
      env:
        RELEASE: ${{ github.ref_name }}
      id: release_tag
      run: |
        echo "tag=${RELEASE%%.*}" >> "$GITHUB_OUTPUT"
        echo "RELEASE=${RELEASE}" >> "$GITHUB_OUTPUT"
    - uses: secondlife/action-autobuild@v3.8.0
      with:
        addrsize: ${{ matrix.addrsize }}
        token: ${{ github.token }}
        build-args: ../build-cmd.sh 1332416345
    - run: |
    echo "Sleeping for 60 seconds to allow for upload"
    sleep(60)
  release:
    needs:
    - package-windows
    - package-macos
    runs-on: [ubuntu-latest]
    steps:
      - uses: secondlife/action-autobuild-release@roxie/webrtc-release-debugging
        with:
          public: false
